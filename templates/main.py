import itertools as it
import functools as ft
import math
from functools import reduce
from itertools import (
    combinations,
    combinations_with_replacement,
    permutations,
    product,
    pairwise,
    repeat,
)

with open("202X/X/input.txt") as f:
    raw = f.readlines()
